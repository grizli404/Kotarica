import 'package:app/screens/conversation/components/message.dart';

class Chat {
  final String name, image, time;
  Message message;
  Chat({
    this.name,
    this.image,
    this.message,
    this.time,
  });
}
